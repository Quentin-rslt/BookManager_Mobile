import { StyleSheet } from "react-native";

export default StyleSheet.create({
    bookContainer: {
        alignItems: "center",
        width: "100%",
    },
    
    booksContainer : {
        justifyContent: 'center',
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
    },
});
